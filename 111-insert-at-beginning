public void insertAtStart(String cityName, List<String> cities) {
    //code here
    cities.add(cities.get(cities.size()-1));
    for(Integer i=0;i<cities.size()-1;i++){
        cities.set(i+1,cities.get(i));
    }
    cities.set(0,cityName);
}