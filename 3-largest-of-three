public static Integer findLargest(Integer num1, Integer num2, Integer num3) {
	//code here
    list<integer> l = new list<integer>{num1,num2,num3};
    l.sort();
    return l[2];
            // num1>=num2 && num1>=num3 ? num1:
            // num2>=num1 && num2>=num3 ? num2:
            // num3;
}